#include "ViewList.hpp"

void ViewList::ShowScreen(Model& _model){
    // system("clear"); // Clear the console

    std::cout << "ViewList::ShowScreen()" << std::endl;

    // CurrentMedia(_model);
}